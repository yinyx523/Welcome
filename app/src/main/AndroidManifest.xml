<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.welcome">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app"
        android:supportsRtl="true"
        android:theme="@style/Theme.Welcome">

        <activity android:name=".login_register.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".login_register.RegisterActivity" />
        <activity android:name=".HomeActivity"/>
        <activity android:name=".bmi.QueryActivity"/>
        <activity android:name=".bmi.BmiActivity"/>
        <activity android:name=".food.FoodDescActivity" />
        <activity android:name=".food.FoodListActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>